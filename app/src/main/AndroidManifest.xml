<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.arduinohackathon_team3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="스마트 SWU쿨"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.ManagerMainActivity"></activity>
        <activity android:name=".Activity.OfficeCheckActivity" />
        <activity android:name=".Activity.ClassroomCheckActivity" />
        <activity android:name=".Activity.ToiletCheckActivity" />
        <activity android:name=".Activity.StudentMainActivity" />
        <activity android:name=".Activity.JoinActivity" />
        <activity android:name=".Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>